using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoolManage : MonoBehaviour
{
    public bool IsStart;
    public bool IsLoad;
    public bool IsPause;
    public bool IsEvent;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
